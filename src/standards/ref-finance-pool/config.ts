export const REF_FINANCE_POOL_TESTNET = 'ref-finance-101.testnet';

const DEFAULT_REF_FINANCE_POOL_METHODS: { viewMethods: string[]; changeMethods: string[] } = {
   viewMethods: ['get_pool'],
   changeMethods: [],
};

export { DEFAULT_REF_FINANCE_POOL_METHODS };
